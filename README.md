# Beer tally Telegram Bot

    1. Download Rust
    ```bash
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
    ```

    2. Install & Initialize Docker
    3. Open Project on VSCode Container
    4. Build and Run main.rs

    ```bash
    cargo build
    cargo run
    ```
